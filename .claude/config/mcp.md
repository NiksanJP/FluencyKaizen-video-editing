# MCP Configuration

Claude Code uses only built-in tools:

- **filesystem** — Read/write project.json and list assets
- **bash** — List directory contents (assets)

No external MCP servers, APIs, or integrations are configured.

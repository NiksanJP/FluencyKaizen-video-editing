#!/usr/bin/env bun
/**
 * Entry point for the unified studio runtime.
 *
 * This file exists so the root package scripts can target a single stable
 * module path while the studio implementation stays organized under src/studio/.
 */
import "./launch.ts";
